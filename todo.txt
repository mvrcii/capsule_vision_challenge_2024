rework loading / finetuning of head / backbone etc
rename fold id to validation fold id
class balancing / capping via arg param / config (currently deactivated)
transforms via configs? (inspired by vesuvius)
add tta check script
